[server]
hosts = 0.0.0.0:5232

[auth]
# Caddy handles password verification and forwards the authenticated username
# in the X-Remote-User header. Radicale trusts that header to identify the user.
type = http_x_remote_user

[storage]
filesystem_folder = /var/lib/radicale/collections
