[server]
hosts = 0.0.0.0:5232

[auth]
# Authentication is handled by Caddy (basicauth).
# Radicale trusts all connections arriving from the reverse proxy.
type = none

[storage]
filesystem_folder = /var/lib/radicale/collections
